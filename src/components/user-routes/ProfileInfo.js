import Base from "../Base";

const ProfileInfo = () => {
    return (
        <Base>
            <div>
                <h1>This is profile Page</h1>
            </div>
        </Base>
    );
}

export default ProfileInfo;